query GetInventories($sort: InventorySort, $filter: InventoryFilter) {
	inventory(sort: $sort, filter: $filter) {
		id
		ingredient {
			id
			name
			category {
				id
				name
				colorCode
			}
		}
		quantity {
			numerator
			denominator
		}
		unit
		expiryDate
		frozen
		status
	}
}
