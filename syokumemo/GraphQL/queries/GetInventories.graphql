query GetInventories($sort: InventorySort) {
	inventory(sort: $sort) {
		id
		ingredient {
			id
			name
			category {
				id
				name
				colorCode
			}
		}
		quantity {
			numerator
			denominator
		}
		unit
		expiryDate
		frozen
		status
	}
}
